import { GameSceneInterface } from './SceneManager';

/** ShopScene — placeholder. Shop functionality is handled by UIScene. */
export class ShopScene implements GameSceneInterface {
  id = 'ShopScene';
  create(): void {}
  shutdown(): void {}
}
